version: '2'
services:

  www:
    build: www/.
    volumes:
      - ./www:/opt/www
    environment:
      - FLASK_APP=microblog.py
      - FLASK_DEBUG=1
    ports:
      - "5000:5000"
    #links:
      #- db
    #depends_on:
      #- db

  #db:
    #build: db/.
    #volumes:
      #- ./db/database:/var/lib/mysql
    #environment:
       #MYSQL_ROOT_PASSWORD: supersecure
